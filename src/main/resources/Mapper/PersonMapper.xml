<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maven.Mapper.PersonMapper">
    <!-- Person getAllPerson();-->
    <select id="getAllPerson" resultType="person">
        select * from tb_person
    </select>
</mapper>